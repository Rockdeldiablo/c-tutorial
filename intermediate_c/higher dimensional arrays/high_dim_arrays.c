#include <stdio.h>

//simulating 2d array with malloc
int main (void){
    int **p;
// if the array is to be x rows, then allocate x many integer pointers.
p = mallo9c (3* sizeof(int *));
//allocate y elements for each row

p[i]= malloc (3* sizeof(int));

/*p is a pointer to the first of x integer pointers.
> *p is of type int *
>p[0] is also of type int*
>p[0][j] is the jth itneger in the row pointed to by p[0]


*/





    return 0;
}